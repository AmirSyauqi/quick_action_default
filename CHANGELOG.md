## 0.0.1

- Initial release of `quick_action_default`.
- Added `QuickActionItem` model.
- Added `QuickActionDefault` widget with `filled`, `outlined`, and `glass` variants.
- Added theme-aware defaults and color overrides.
- Added example app under `example/`.
